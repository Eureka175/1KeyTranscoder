# Runtime logs / reports
logs/
*.log
*.csv

# 例外: 通道同步 P1 真实素材冻结基线 (纯文本 fixture, 需入库; 不含媒体)
!tests/fixtures/channel_sync/*.csv

# Temporary / partial outputs
*.part
*.part.mov
*.part.mkv
*.encode.log

# Generated outputs (anchored to the repo root: a bare `archive/` would also
# match docs/archive/, silently ignoring the documentation archive area)
/archive/
/output/
/testout/
results/

# Python
__pycache__/
*.py[cod]

# Local/editor
.vscode/
.idea/

# Local test media
testsets/
tools/

# Forensic reports / generated inspection logs
metadata_forensics/

# Preservation POC intermediates
work/

# Release build output (staging tree + archive + manifest)
dist/

# PyInstaller scratch tree (x265 calibration tooling)
/build/

# Third-party source checkouts / toolchains used as build inputs by the research
# experiment line: rigaya/NVEnc + rigaya/QSVEnc pinned checkouts, CUDA
# headers+libs from redistributable wheels, AviSynth+/VapourSynth/OpenVINO/OpenCL
# SDK stubs, downloaded archives. Large, licensed separately, never tracked.
third_party/
